# PROTOCOL-VERSION — single stamp for coordination-protocol docs AND scripts.
# Bump when amending README or any shipped *.sh behavior that seats depend on.
# Scripts source this file (or embed the same string) so `coord-status` / monitor
# arm banners can prove they match the docs a human is reading.
PROTOCOL_VERSION=1.6.0
