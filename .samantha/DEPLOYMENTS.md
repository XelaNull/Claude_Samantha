# Samantha Prime — Deployment Catalog

**What this is.** A registry of every place the Samantha Prime framework (this repo) is installed downstream, so that when canon changes here we know exactly *where* to propagate and *what to watch for* at each site. This repo (`Claude_Samantha`) is the **canonical source**; every deployment derives from it.

**Keep this current.** Update the table + change log whenever you install into a new location, migrate a coordination generation, or discover a per-site customization. A stale catalog is worse than none — it reads as "fully mapped" when it isn't.

_Last verified: **2026-08-03** (deep cleanup: deleted retired coord scripts; pack citation/tier docs; plan/spec tombstones)._

---

## Deployment map

| Path | Role | Cluster | Coord | M9 identity | Git repo | Output-style | Customization to preserve |
|------|------|---------|-------|-------------|----------|--------------|---------------------------|
| `github/Claude_Samantha/` | **Canonical source** | — | none | — | yes | 263L (source of truth) | — |
| `github/Nebuspace/` | Orchestrator | Nebuspace dual | M9 | `orchestrator` | no (workspace root) | 263L ✓ | none |
| `github/Nebuspace/Sectorwars2102/` | Implementer | Nebuspace dual | M9 | `impl-sectorwars` | yes | 269L ✓ | **Project-Specific Context** — distilled Sectorwars specifics (Mac-vs-remote execution boundary, additive-only migrations, `dev`/`master` git workflow, Scroll Law, proving standard, `~/github/ServerSetup/` topology). MERGE, never blind-overwrite. |
| `github/XelaNull/` | Orchestrator | XelaNull/pushling dual | M9 | `orchestrator` | no (workspace root) | 263L ✓ | none |
| `github/XelaNull/pushling/` | Implementer | XelaNull/pushling dual | M9 | `impl-pushling` | yes | 284L ✓ | **Project-Specific Context** — build/run/reload/install cmds, embodiment pitfalls, **TS-500 `mcp/` code-quality overlay**. MERGE, never blind-overwrite. |
| `github/Nebuspace/tw2002-aiclient/` | **Implementer** | Nebuspace dual (joined 2026-07-19; **reinstalled 2026-07-23** after rebirth wiped framework dirs) | M9 | `impl-tw2002` | yes (git; `.claude/`/`.samantha/` gitignored — hub ruling) | 268L ✓ | **Project-Specific Context** — rebirth layout (`canon/` + `workorders/`; product code under `archive/pre-rebirth-2026-07-23/` until WOs restore root), north-star roles (human/app/AI-teacher), secrets doctrine pointer, no-linter rule, WO-only build path, seat definition `impl-tw2002`. Also `monk`/`mack`/`cipher`/`pixel` extensions filled for rebirth; rook/rosetta generic. MERGE, never blind-overwrite. |
| `github/AIBudget2026/` | **Standalone (solo)** | — (no coordination) | none | — | not git | 289L ✓ | **Project-Specific Context** — vanilla-JS/Python household-budget app, build/seed/serve cmds, **privacy-first pitfalls**. Also: `monk` + `cipher` extensions filled (cipher = PII/egress threat model); `CLAUDE.md` carries a solo-mode banner (existing rich project context preserved). **🔒 Holds real financial PII incl. minors — local-only, not git; sanitize/gitignore before any `git init`.** MERGE, never blind-overwrite. |
| `github/claude-hud-weeklyToDaily/` | **Standalone (solo)** | — (no coordination) | none | — | yes (`XelaNull/claude-hud-weeklyToDaily`) | filled ✓ | **Project-Specific Context** — TypeScript Claude Code statusline HUD fork (weekly→daily usage theme); `npm run build` / `npm test`; solo banner on `CLAUDE.md`; `monk`/`mack`/`cipher` extensions filled; rook/pixel/rosetta generic. Zero coord wiring. Note: `.gitignore` already ignores `.claude/settings.json` (persona still on disk). MERGE, never blind-overwrite. |

**Coord-dirs (M9 star-topology, per cluster):**
- Nebuspace dual → `/Users/mrathbone/github/Nebuspace/.samantha/coord/`
- XelaNull/pushling dual → `/Users/mrathbone/github/XelaNull/.samantha/coord/`

**Managed-but-NOT-installed** (the Nebuspace Orchestrator issues work orders into these sibling repos, but they carry no framework install of their own — no output-style, no agents; the Orchestrator instance runs from the Nebuspace root):
- `github/Nebuspace/sw2102-bang/` — galaxy-generation sidecar
- `github/Nebuspace/sw2102-docs/` — canonical game spec (**PUBLIC** — auto-deploys to Cloudflare on push to `main`; never write secrets/coordination chatter here)

**Coordination generation:** both duals are on **M9 STAR-topology**. No gen-1 deployments remain (Nebuspace ran gen-1 until its 2026-07-05 migration; XelaNull/pushling was built on M9 from the start).

---

## What "the framework" is — the sync inventory

When propagating an update, these are the artifacts that travel. Copy from `Claude_Samantha/` unless noted.

| Artifact | Path | Notes |
|----------|------|-------|
| Persona (output-style) | `.claude/output-styles/samantha.md` | The **hand-edited source of truth** — identity + operating rules. Claude Code loads it via `outputStyle`. Does **not** cascade; each install needs the full file locally. Preserve any filled `## Project-Specific Context`. |
| Persona (Cursor bridge) | `.cursor/rules/samantha.mdc` | Always Apply rule for **Cursor Agent** (Cursor ignores `outputStyle`). **Generated** from the output-style via `.samantha/references/templates/sync-cursor-persona.sh` — never hand-edit; re-sync after overlay edits. |
| Agent defs (×6) | `.claude/agents/*.md` | monk, rook, mack, cipher, pixel, rosetta. Preserve any filled `## Project-Specific Extensions`. |
| Skills (×16) | `.claude/skills/*/` | Portfolio-portable as shipped. |
| Settings | `.claude/settings.json` | Sets `outputStyle` (Claude Code only), wires the PreToolUse coordination hook. **The hook needs per-seat args** (see lessons). |
| M9 coordination scripts | `.claude/coord-monitor.sh`, `coord-send.sh`, `coord-status.sh`, `heartbeat.sh`, `bootstrap-identity.sh`, `coordination-precommit-hook.sh`, and (PROTOCOL-VERSION 1.1.0) `coord-protocol-metrics.sh`, `coord-session-healthcheck.sh`, `coord-evidence-lint.sh`, `coord-gate-audit.sh` + `PROTOCOL-AMENDMENTS.tsv` | Live copies at each **workspace root** (`Nebuspace/.claude/`, `XelaNull/.claude/`). Canonical source = `.samantha/references/coordination-protocol/`. Implementer sub-repos invoke the parent's copies by absolute path. **Solo installs: do not copy.** Retired `watch-coordination.sh` / `git-pre-commit.sh` are **tombstones only** under `retired/` (executables deleted — do not resurrect). |
| Reference Pack | `.samantha/references/` | OKF format, coordination-protocol (M9), adr-process, canonical-docs-system, safety-carveouts, templates. |
| Project context | `CLAUDE.md` | Per-project. Coordination section is M9 topology; the rest is project-specific — **author/preserve carefully**, don't blind-copy. |

---

## Update procedure (propagating a canon change)

1. **Change canon here first** (`Claude_Samantha/`), commit, and merge to `main`. This repo is the source; deployments follow.
2. **Classify the change:**
   - **Additive file copies** — output-style, agents, skills, references. Safe to copy into a deployment even while its instances are live; they adopt it on **next restart**.
   - **Replacing coordination deploy** — swapping the `.claude/*.sh` coordination scripts, or rewriting a `CLAUDE.md` coordination section, or migrating a coordination generation. This is **NOT** a safe hot-copy → **stop the instances first**, migrate, restart onto the new protocol.
3. **Per deployment, before copying — read the lessons below**, then copy, then verify on disk.
4. **After propagation:** instances pick up output-style / CLAUDE.md changes on their **next session start**. Note that in your report so the human knows a restart is required.

### Hard-won lessons (do not relearn these the hard way)

- **Check customizations before overwriting.** A filled `## Project-Specific Context` (pushling), an agent's `## Project-Specific Extensions`, a skill/code-quality overlay — a blind copy wipes them. Diff first (empty-template vs filled); **merge**, don't clobber. → `pushling` is the live example (its build commands + TS-500 overlay). _(memory: check-customizations-before-overwriting-deployed-framework)_
- **Output-styles don't cascade.** Unlike `CLAUDE.md`, the output-style is a single selected file loaded from the nearest `.claude/output-styles/`. An implementer sub-repo does **not** inherit the workspace-root style — it needs the **full** persona locally (identity + all operating rules). Never split rules to the parent and assume inheritance (this was the original pushling install bug, fixed 2026-07-05).
- **Cursor ignores `outputStyle` / `.claude/output-styles/`.** Persona in Cursor requires the Always Apply bridge `.cursor/rules/samantha.mdc`, generated from the local output-style by `sync-cursor-persona.sh`. After filling/merging Project-Specific Context, re-sync. Sites that gitignore `.claude/` as local orchestration should gitignore `.cursor/` the same way (still install the rule on disk).
- **Stop live instances before a replacing coordination deploy.** Verify no live instance first (mailbox tail + running watchers). A live pair mid-work will have its coordination broken by a script/CLAUDE.md swap. _(memory: stop-live-instances-before-replacing-deploy)_
- **Never run git in a live shared repo.** Copy files (safe); let the owning Orchestrator or the human do the commit. _(memory: no-git-ops-in-live-shared-repo)_
- **A nested standalone install needs an ancestor-cascade carve-out.** `CLAUDE.md` files cascade from ancestor directories. A standalone install nested *inside* a coordination workspace (tw2002-aiclient under `Nebuspace/`) inherits the workspace root's live M9 orchestrator context — whose WHO-AM-I boot table doesn't list the new repo — so a session there can misidentify as a coordination seat. Fix at install time, in the target only: an explicit "Ancestor-context override" in the target `CLAUDE.md` **and** a mirroring bullet in the output-style's Project-Specific Context (never edit the live workspace root's `CLAUDE.md` for this — that's a replacing coordination change).\n- **The coordination hook needs per-seat args.** Wire `settings.json` as `<hook-path> <coord-dir> <this-seat's-identity>` (Orchestrator → `orchestrator`; Implementer → `impl-<name>`). Bare-path leaves the mailbox-read gate + roster dump silently skipped. _(memory: m9-hook-needs-args-per-seat)_

---

## Change log

- **2026-08-03 (later)** — **Deep cleanup — no footgun leftovers.** Deleted `retired/watch-coordination.sh` and `retired/git-pre-commit.sh` from the reference pack (tombstone README only; bodies remain in git). Heartbeat dead-man rationale rewritten for the persistent `coord-monitor.sh` (WATCHER-DOWN re-arm no longer passes `--role`). SUPERSEDED plan shrunk to a stub; draft spec bannered HISTORICAL / not an installer guide. Pack citations: Disaster Rules **6** (was "5"); ROSTER `coord-monitor`; solo vs dual **script tiers**; self-contained § Arming the inbox (no dangling workspace CLAUDE.md cite); sqlite advanced sketch aligned to persistent monitor.
- **2026-08-03 (later)** — **Canon polish pass.** Fixed README skill count ("Fourteen" → "Sixteen"); updated live `heartbeat.sh` + precommit-hook comments/re-arm path so WATCHER-DOWN instructions point at `coord-monitor.sh` (not the retired `watch-coordination.sh`; PID-compat for the retired name retained). Left historical SUPERSEDED plan/spec archaeology intact.
- **2026-08-02 (later)** — **Agent frontmatter discovery hardening (Cursor + Claude Code).** Rewrote all six agent `description` fields to third-person WHAT+WHEN with explicit "Use proactively…" trigger phrases and Mack↔Cipher boundary cues; added Cursor `readonly: true` on rook/mack/cipher/pixel (tools allowlist unchanged for Claude Code). Frontmatter-only propagate to registered deployments — Project-Specific Extensions bodies preserved.
- **2026-08-02 (later)** — **Skill discovery + banner hardening.** All 16 skills already had activation banners; rare visible skill use was mostly weak auto-invoke copy. Rewrote every `description` to third-person WHAT+WHEN with richer trigger phrases (Cursor + Claude Code); strengthened the REQUIRED first-output banner rule; fixed `gate`'s conflicting "don't announce" guidance; updated SKILL template + persona Skill Routing ("engage the matching skill, don't freestyle"); propagated skills to registered deployments and re-synced Cursor persona rules.
- **2026-08-03** — **Coordination-protocol robustness amendment (framework 2.2.0), backported from a live Nebuspace ratification cycle.** `PROTOCOL-VERSION` 1.0.0 → 1.1.0 in the coordination-protocol reference pack. Row-hygiene queue columns (`gated`/`schema`/`verified-against`, three gate kinds), the evidence rule (unevidenced external-artifact-state claims banned, positive or negative), new scripts `coord-protocol-metrics.sh` / `coord-session-healthcheck.sh` / `coord-evidence-lint.sh` / `coord-gate-audit.sh`, a queue-depth heartbeat tick, and a fail-closed gated-push guard + reverse-index advisory in the precommit hook. Also fixed a pre-existing leak: `coord-status.sh`/`coord-send.sh`/`coord-monitor.sh` hardcoded an absolute deployment-specific path as their default coord-dir, now `${COORD_DIR:-$PWD/.samantha/coord}`. Project-specific WO IDs/queue rows/chronology stripped; every project-named root generalized to a required flag with no default. **Not yet rolled out to individual deployments** (Nebuspace, XelaNull, pushling, tw2002-aiclient, claude-hud-weeklyToDaily, AIBudget2026) — this entry covers canon only; propagation is separate follow-up work per the Update procedure above.
- **2026-08-02** — **claude-hud-weeklyToDaily standalone (solo) Mode A install.** Full framework from this canon into `/Users/mrathbone/github/claude-hud-weeklyToDaily` (git fork of jarrodwatts/claude-hud): persona + Project-Specific Context, 6 agents (monk/mack/cipher overlays filled; rook/pixel/rosetta generic), 16 skills, Reference Pack, seeded project memory, `settings.json` = `outputStyle: Samantha` only, `settings.local.json` hooks, Cursor bridge via `sync-cursor-persona.sh`. **Zero coordination wiring.** Solo banner prepended to existing `CLAUDE.md`. GLOBAL `~/.samantha/MEMORY.md` already existed — skipped. This catalog row's commit remains Max-gated.
- **2026-07-23 (later)** — **Cursor persona bridge (framework 2.1.1).** Cursor Agent does not honor Claude Code `outputStyle` / `.claude/output-styles/`. Added `.samantha/references/templates/sync-cursor-persona.sh` to generate Always Apply `.cursor/rules/samantha.mdc` from each install's local output-style (preserves Project-Specific Context). Canon commits the generated rule; install/adoption docs + sync inventory updated. Rolled out (additive) to all registered deployments. New chats required for the rule to load.
- **2026-07-23** — **tw2002-aiclient framework reinstall after rebirth.** Product rebirth archived the prior tree (incl. `.claude/` / `.samantha/`); live root is now `canon/` + `workorders/` + git. Reinstalled Mode A from this canon: persona (268L), 6 agents (monk/mack/cipher/pixel overlays rewritten for rebirth; rook/rosetta generic), 16 skills, Reference Pack, seeded memory, `settings.local.json` hooks, M9 per-seat `settings.json` (`impl-tw2002` + worker-git-deny — parent scripts by absolute path). Authored slim `CLAUDE.md` (doc-canon, WO-only work path, hard rules, Implementer coordination). Did **not** rewrite Nebuspace coord presence files or parent WHO-AM-I (live coordination state). GLOBAL `~/.samantha/MEMORY.md` already existed — skipped. Framework dirs remain gitignored in the target.
- **2026-07-19 (later)** — **tw2002-aiclient joined the Nebuspace dual as `impl-tw2002` (Max-ratified live, same day as the solo install).** Sequence followed the install's own carve-out rule: seat registered HERE first, then the repo's `CLAUDE.md` ancestor-context override + output-style mirror bullet rewritten from "NOT a seat" to the seat definition, per-seat coordination hook wired in the repo's `.claude/settings.json` (Sectorwars `SAMANTHA_IDENTITY` arg pattern; effective next session start), `impl-tw2002.md` presence file written to the Nebuspace coord-dir, monitor + heartbeat armed. **Parent `Nebuspace/CLAUDE.md` WHO-AM-I table deliberately left untouched** (replacing coordination change — hub/Max to schedule; the hub auto-discovers the new seat file meanwhile, and the repo `CLAUDE.md` is the authoritative WHO-AM-I answer for its own cwd until the table is amended). Repo is still not-git → Rule 1/6 commit disciplines moot until a `git init` (hub/Max decision). This file's commit remains Max-gated.
- **2026-07-19** — **tw2002-aiclient standalone (solo) install.** Second non-dual deployment (AIBudget2026 pattern): full framework — persona (272L: 263L canon + filled Project-Specific Context), 6 agents (monk/mack/cipher/pixel extensions filled from verified code; rook/rosetta byte-identical to canon per the adoption guide), 16 skills, Reference Pack, seeded `.samantha/` memory, generic hooks (`settings.local.json`) — with **zero coordination wiring**. Authored the project `CLAUDE.md` (doc-canon ordering, verified commands, architecture map, hard rules). All commands verified live (pytest --collect-only: 390; `./tw status`). Pre-existing harness-native `.claude/agent-memory/monk/` preserved checksum-identical. **New hard-won lesson recorded above:** the install sits *inside* the Nebuspace workspace, so the live M9 orchestrator `Nebuspace/CLAUDE.md` cascades into its sessions — an explicit ancestor-context override was added to the target `CLAUDE.md` + output-style so sessions never misidentify as a coordination seat (the parent's WHO-AM-I table was deliberately left untouched — updating it is a replacing change for the human/orchestrator to schedule). Deferred decisions surfaced to the human: dead `twclient/credstore.py`, stale README spec pointer, TUI-POLISH-PLAN status conflict, a real name present in the project's own pre-existing docs. Note: a live `twd` daemon was connected throughout — install was purely additive, so the framework activates on the next session start in that repo.
- **2026-07-18** — **watch-coordination.sh → coord-monitor.sh cutover completed across all deployments.** A fresh instance had armed the retired one-shot echo-and-terminate watcher after reading a stale reference pack, exposing that the coordination-protocol docs and reference packs still presented `watch-coordination.sh` as canonical everywhere except the live `Nebuspace/.claude/` scripts. Added `--force-poll` to `coord-monitor.sh` (network-mounted coord-dirs, where local fs-events never see a remote peer's write — live swap via atomic temp-then-`mv`, the running Nebuspace orchestrator monitor was never interrupted). Synced the current suite (`coord-monitor.sh`, `coord-send.sh`, `coord-status.sh`, `heartbeat.sh`, `bootstrap-identity.sh`, `coordination-precommit-hook.sh`) into `Nebuspace/.claude/`, `XelaNull/.claude/`, and all three `.samantha/references/coordination-protocol/` packs (Nebuspace, Claude_Samantha, XelaNull — including the previously-unsynced `XelaNull/pushling/.samantha/references/coordination-protocol/` copy). `watch-coordination.sh` retired to a `retired/` subdir with a tombstone README in every location it lived. Fixed the remaining prescriptive arming instructions in `Nebuspace`'s reference README, `XelaNull/CLAUDE.md`, and `XelaNull/pushling/CLAUDE.md` to the persistent arm-once-per-session model (no more "re-arm as your last action of a wake-cycle" — that was specific to the retired one-shot watcher). `XelaNull/.samantha/coord/CONTINUITY.md` and `QUEUE.md` were read and left untouched (historical/state records, not prescriptive). This row's `.claude/watch-coordination.sh` reference above is now historical only — see `retired/README.md` in any reference pack.
- **2026-07-05** — **OKF reference accuracy fix.** Audited `okf-format.md` against Google's authoritative `okf/SPEC.md`: faithful except one over-hardened normative verb — the §4.1 extensibility clause said consumers "MUST NOT reject" unknown-field documents where the spec says "SHOULD NOT" (the MUST-NOT belongs only to the bundle-conformance clause, which was already correct). Corrected in canon + propagated the one-line fix to all 5 deployments (byte-identical OKF docs kept in sync).
- **2026-07-05** — **AIBudget2026 standalone (solo) install.** First non-dual deployment: full framework (persona, 6 agents, 16 skills, reference pack, seeded memory) with **zero coordination wiring** — settings = `{"outputStyle":"Samantha"}` only, no coord-dir, no scripts, no precommit hook, no dual-instance CLAUDE.md section (the reference pack's coordination-protocol travels along inert, available if it ever goes dual). Filled the output-style Project-Specific Context + `monk`/`cipher` agent extensions; added a non-destructive solo-mode banner to the existing `CLAUDE.md`. **Privacy-flagged:** repo holds real financial PII incl. minors, is local-only + not git — sanitize/gitignore before any `git init`.
- **2026-07-05** — **Nebuspace dual migrated gen-1 → M9.** Installed M9 scripts (dead-man switch, singleton guard) into `Nebuspace/.claude/`; created `Nebuspace/.samantha/coord/`; rewrote both `CLAUDE.md` coordination sections to M9 STAR; wired the precommit hook per-seat (`orchestrator` / `impl-sectorwars`); archived the gen-1 mailbox + ROSTER (`ROSTER.gen1-archive.md`, `Sectorwars2102/CROSS-CLAUDE.gen1-archive.md`). Output-style + references synced to 263L.
- **2026-07-05** — **XelaNull/pushling dual hardened.** Fixed the Orchestrator hook (bare-path → args) and added the missing Implementer hook (`pushling/.claude/settings.json`, `impl-pushling`). Resynced output-styles: XelaNull 263L (clean), pushling 284L (**merged** — full canon body + preserved Project-Specific Context; removed the obsolete "promoted to framework, not duplicated here" note that had left pushling running without the Standing Working Rules).
- **2026-07-05** — **Implementer parity closed.** Filled Sectorwars2102's output-style `## Project-Specific Context` (269L) — distilled from its own `CLAUDE.md` — so the Nebuspace dual's Implementer now matches the XelaNull dual's (pushling) in carrying its project overlay in the always-on persona layer, not just in `CLAUDE.md`. Orchestrator roots correctly stay empty on both duals.
- **_(earlier, 2026-07)_** — OKF adopted as canonical AI-knowledge format (replaced `.aispec`) and propagated; pushling full install; XelaNull dual setup built on M9; coordination hardening merged (PR#1 heartbeat v2.1, PR#2 watcher v2.2 singleton guard); persona: Standing Working Rules always-on + Reference Library (OKF progressive-disclosure) pointer.
